---
apiVersion: v1
kind: List
items:
- apiVersion: v1
  kind: Service
  metadata:
    annotations:
      discovery.3scale.net/discovery-version: v1
      discovery.3scale.net/description-path: /openapi.json
      discovery.3scale.net/scheme: http
      discovery.3scale.net/port: "8080"
      fabric8.io/iconUrl: img/icons/camel.svg
      prometheus.io/port: "9779"
      prometheus.io/scrape: "true"
    labels:
      expose: "true"
      discovery.3scale.net: "true"
      app: task-api
      provider: fabric8
      version: 1.0-SNAPSHOT
      group: io.example
    name: task-api
  spec:
    ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
    selector:
      app: task-api
      provider: fabric8
      group: io.example
- apiVersion: apps.openshift.io/v1
  kind: DeploymentConfig
  metadata:
    annotations:
      fabric8.io/iconUrl: img/icons/camel.svg
      fabric8.io/metrics-path: dashboard/file/camel-routes.json/?var-project=task-api&var-version=1.0-SNAPSHOT
    labels:
      app: task-api
      provider: fabric8
      version: 1.0-SNAPSHOT
      group: io.example
    name: task-api
  spec:
    replicas: 1
    revisionHistoryLimit: 2
    selector:
      app: task-api
      provider: fabric8
      group: io.example
    strategy:
      rollingParams:
        timeoutSeconds: 3600
      type: Rolling
    template:
      metadata:
        annotations:
          fabric8.io/iconUrl: img/icons/camel.svg
          fabric8.io/metrics-path: dashboard/file/camel-routes.json/?var-project=task-api&var-version=1.0-SNAPSHOT
        labels:
          app: task-api
          provider: fabric8
          version: 1.0-SNAPSHOT
          group: io.example
      spec:
        containers:
        - env:
          - name: KUBERNETES_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          image: example/task-api:snapshot-191012-160606-0310
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
              scheme: HTTP
            initialDelaySeconds: 180
          name: spring-boot
          ports:
          - containerPort: 8080
            name: http
            protocol: TCP
          - containerPort: 9779
            name: prometheus
            protocol: TCP
          - containerPort: 8778
            name: jolokia
            protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
              scheme: HTTP
            initialDelaySeconds: 10
          resources:
            limits:
              cpu: "1.0"
            requests:
              cpu: "0.2"
          securityContext:
            privileged: false
    triggers:
    - type: ConfigChange
- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    labels:
      expose: "true"
      app: task-api
      provider: fabric8
      version: 1.0-SNAPSHOT
      group: io.example
    name: task-api
  spec:
    port:
      targetPort: 8080
    to:
      kind: Service
      name: task-api
